upstream yellowpages_app {
    server yellowpages-app:8000;
}

server {
    listen 80;
    server_name _;   # replace with your domain, e.g. dashboard.example.com

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_pass         http://yellowpages_app;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }
}

# After certbot issues a cert, uncomment and set your domain (see DEVOPS.md).
# server {
#     listen 443 ssl;
#     http2 on;
#     server_name dashboard.example.com;
#     ssl_certificate     /etc/letsencrypt/live/dashboard.example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/dashboard.example.com/privkey.pem;
#     location / {
#         proxy_pass         http://yellowpages_app;
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#     }
# }
